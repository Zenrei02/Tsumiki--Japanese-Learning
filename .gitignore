# ─────────────────────────────────────────────────────────────────────────────
# Naoshi — what does not belong in version control
#
# ⚠️ MAKE THE REPOSITORY PRIVATE. Not a preference. See "Never public" below —
# the folder contains a third party's personal messages and another person's
# voice recordings, and the audio contract's signature lines are still blank.
# ─────────────────────────────────────────────────────────────────────────────

# ── Build output and dependencies ───────────────────────────────────────────
node_modules/
naoshi-app/node_modules/
naoshi-app/dist/
dist/

# ── The Vite app's generated source IS COMMITTED. Do not ignore it. ─────────
#
# An earlier version of this file excluded naoshi-app/src/App.jsx, main.jsx,
# modules/, data/ and the generated lib/ files, on the ordinary principle that
# generated output does not belong in version control.
#
# That principle assumes the generator runs in CI. This one does not: it is a
# PYTHON script that reads the single-file artifacts, and Netlify's build only
# runs `npm run build`. Ignoring them shipped a repository with no application
# source, and the deploy failed with "Failed to resolve /src/main.jsx" and
# "0 modules transformed".
#
# So they are tracked. They are generated *and* they are the deployable source;
# the generator is a local authoring tool, not part of the deploy pipeline.
# Workflow: edit a source module in the parent folder, re-run
# build-vite-app.py, commit the result.
#
# (If the build should ever regenerate them instead, the Netlify image does
# carry Python — the build command would become
# `python3 ../build-vite-app.py && npm run build`. Not done: it makes the deploy
# depend on the whole analysis pipeline, including vocab-data-v1.json and
# kanji-order-v2.json, and fails confusingly if any of it drifts.)

# Standalone single-file builds — ~1.2 MB each, regenerated by
# build-standalone-html.py and build-artifact-bundle.py.
*-artifact.jsx
*-standalone.html
shared-stroke-engine.js
vocab-data-v1.js

# ── Never public, and think twice even in a private repo ────────────────────
#
# Chat Logs/ holds real conversations with named individuals. That is other
# people's personal data, shared with Lloyd and not with a hosting provider.
# Excluded outright rather than trusted to repository settings.
Chat Logs/

# Raw voice recordings. The contributor agreement's Article 7 excludes
# redistributing the raw audio; Article 5 (compensation), Article 8 (credit)
# and every signature line are still blank as of Session 8. Until that is
# signed, the masters stay off any hosted service.
Audio/Raw (unedited)/
Audio/Kana/
Audio/Kana.bak-*/
*.wav

# ── The kana audio sprite: PUBLISHED ────────────────────────────────────────
# ENABLED Session 9. The contributor gave approval in chat for use in the app;
# Lloyd is retaining that conversation as the record and will obtain the written
# signature later — the two are countries apart. The sprite is therefore COMMITTED
# and ships with every deploy (~750 KB).
#
# Still outstanding on the paper agreement, and worth closing when convenient:
# Article 5 (compensation), Article 8 (credit preference), and the Article 2
# scope points Session 8 found — 拗音 scoped at 33 but 36 delivered, and the
# extended katakana set (vu, fa, fo, ti, tu, che) never in scope at all.
# (No ignore rules here — the files are tracked deliberately.)

# The eval workbook carries the answer key. It is the thing the blind grading
# is blind to — a leaked copy invalidates the eval, not just the privacy.
naoshi-eval-v1.xlsx
naoshi-eval-v1-with-outputs.xlsx
bakeoff-log.jsonl
# Same rule: this holds the raw body of every response the harness could not
# parse, which is model output on eval sentences — the blind material again.
bakeoff-parse-failures.jsonl

# Same rule, same reason: the Step 2 response files carry the reviewer's verdicts
# and their proposed corrections, which IS key material. Added Aug 12 2026 after a
# provisional import copy landed in OLD/.
*responses*.xlsx
OLD/keycheck-responses-*.xlsx

# Step 3 reports. Same rule again: these quote eval sentences, the reviewer's
# comments and the key amendments verbatim, which is the blind material. Added
# Sep 5 2026 with the first one.
step3-import-report-*.md

# Workbook snapshots. Same key material as the workbook itself, so same rule.
# They exist because the workbook is NOT in git — it is the only artefact in this
# project with no version history, so a bad edit is otherwise unrecoverable.
backups/

# The Apps Script form sources. SAME RULE AS THE WORKBOOK, arrived at by a
# different route and missed for months: these are GENERATED from the workbook
# and each one embeds the key material verbatim, so the four rules above were
# being enforced on the source and ignored on its output. Found Aug 23 2026 by
# sweeping every tracked file for the 50 eval sentences rather than by filename.
#
#   build-grading-forms.gs     150 records: sentence + key (判定・修正案・理由,
#                              including the reviewer's Step 2 corrections)
#                              + the full model output being graded
#   build-key-check-forms.gs    50 records: sentence + tier + correction +
#                              rationale + status — status is the ERROR/CORRECT
#                              ground truth the false-positive rate is measured
#                              against, so this one leaks the labels too
#   patch-b7-b8-forms.gs        11 records, same shape
#   build-b9-form.gs            10 records, same shape
#
# The generators (*.py) stay tracked — they carry no key material, and the .gs
# is reproducible by re-running them against the workbook.
#
# THE RULE THIS ENCODES: a file is key material if its CONTENT is, regardless of
# how it got there. Before tracking anything generated from the workbook, grep it
# for eval sentences first.
build-grading-forms.gs
build-key-check-forms.gs
patch-b7-b8-forms.gs
build-b9-form.gs

# ── Local noise ─────────────────────────────────────────────────────────────
.~lock.*#
*.tmp
.DS_Store
__pycache__/
*.py[cod]
OLD/*.scratch*
ANTHROPIC_API_KEY
SETUP KEY
