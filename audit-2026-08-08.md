# tsumiki weekly audit — Saturday 8 August 2026

First run of the scheduled audit. Sources: Session 8 and 9 journals, the Session 9 handover, `curriculum-revisit-map-v1.md` § Audit queue, the AUDIT QUEUE tracker row, the Links page, and one tracker query (cached for the session). Session 9 was still open at run time and its journal carries explicit notes for this audit — the four "looks like a defect, is not" items and the superseded early figures were honoured and are not flagged below.

---

## 1 · Needs a decision from Lloyd

**1. "Spotlight" naming collision** (audit queue item 3). `grammar-module.jsx` L373–374 — the `wa` lesson opens "は is a spotlight" in both `what` and `build`. Still the only two occurrences of the word in the project. If the Spotlight feature keeps its name, the learner-facing word means two things, one of them in the first particle lesson. Two-line fix once you pick which name moves. (File renamed from `n5-practice.jsx` since the item was filed; location re-verified.)

**2. Cross-module particle-pronunciation gap** (audit queue item 2). `hiragana-module.jsx` has the `particles` skill lesson (「は・へ・を」); the grammar module's Step 2 has no counterpart — the quirk lives in one clause of `wa`'s `build` field. Authoring it is new curriculum → reviewer queue. The prior question is yours: **may revisits span modules?** Nothing else in the revisit map crosses one.

**3. Learner feedback form response sheet URL is still unrecorded** on the Links page. The Step 2 key-check response spreadsheet — the critical-path one — *is* now recorded there. This one remains a placeholder; same recovery route as before (open the form's Responses tab from the owning account, paste the sheet URL onto the Links page).

## 2 · Proposed content corrections

**2.1 — hiragana-module.jsx, line 190, H-Series `exp`** *(Japanese teaching content → reviewer queue, not Lloyd's say-so alone)*

- Current: `"…This row matters again later, because three of its kana take on second jobs as particles — and one of them changes how it is read."`
- Suggested: `"…This row matters again later, because two of its kana take on second jobs as particles — and both change how they are read."`
- Why: the H-series is は ひ ふ へ ほ. Two of its kana do particle duty (は, へ) and both change reading (は→wa, へ→e); を is W-series. The "three" leaked from the (correct) note below about は・へ・を getting their own lesson.
- Note: queue said ~L166; the line now sits at 190 after Session 8/9 edits. Text unchanged.

**2.2 — curriculum-revisit-map-v1.md, lines 3 and 130** *(doc housekeeping, your OK is enough — no Japanese content)*

- Current: "next authoring pass on `n5-practice.jsx`" (L3) and "Naming collision — `n5-practice.jsx`, `wa` lesson" (L130).
- Suggested: `grammar-module.jsx` in both places.
- Why: the file was renamed in Session 9. This map is an active working doc ("read before the next authoring pass"), not a historical spec, so the deliberate-staleness exemption doesn't obviously cover it.

## 3 · Fixed at source — tracker rows

All four fixed by fetching the row and re-sending existing Notes with a dated entry **appended** (nothing replaced). Statuses changed where marked.

1. **Vocabulary module** — Status `Not started` → `In progress`. Appended: first draft built overnight (Session 9), compiles, simulated, never rendered; UI review tracked on its own row; open item (1) answered in the build as `known-words-v1`, unconfirmed.
2. **Schedule word recurrence — it is not happening for free** — Status `Not started` → `In progress`. Appended: the row's closing directive ("design separately — not as a side effect of ordering") was superseded later the same session by Lloyd's close decision — recurrence lives in the vocabulary module's return mechanic + the interval-ladder proposal. Corpus starvation (7 words return / 8 events) and the promotion-queue expansion remain open.
3. **AUDIT QUEUE — Saturday Aug 8 2026** — Status `Not started` → `In progress`. Appended: all three items verified live; item 1 confirmed at L190 and routed to the reviewer via this report; items 2–3 put to Lloyd; kana numeric verification stands.
4. **Revisit map — apply pain-point resurfacing systematically** — Status unchanged. Appended: filename rename note, plus the verification that all 14 lesson ids and 12 step references in the map match the live module.

## 4 · Checked, clean

- **Revisit map integrity**: all 14 lesson ids it references exist (grammar, hiragana, kanji), and all 12 step-number claims match the live `cat:` headers in `grammar-module.jsx` (wa/sb-waga/sb-nide → Step 2, sb-pronouns → 3, teiru/teiru2/sb-transitive → 4, ageru → 8, potential/sb-ganotwo → 10, sb-waga2 → 11, tara → 12). No stale step references found.
- **tsumiki-prototype.jsx**: `SYSTEM_PROMPT` present, schema `naoshi-4` intact; the Session 9 edit touched a comment only (the `grammar-module.jsx` reference). Phase 0 freeze respected.
- **Kana numeric claims**: the ~40-claim verification from Aug 6–7 stands. Both kana files changed since (Session 8 audio/yōon wiring, Session 9 comment rename), but the additions are data tables and romaji keys — no new prose counting claims found. The H-series line (§2.1) remains the only known error.
- **Tracker rows checked and not drifted**: *Run the model bake-off* (still blocked on API credits + key verification — accurate), *Recompute kanji coverage curve* (still blocked on corpus counts — accurate; Session 9's sensitivity sweep retired the *ordering* dependency, not the curve's), *Consolidate three word stores* (286-word count consistent with today's regenerated ledger), and Session 9's three new rows (*first UI review*, *retention model proposal*, *apply kanji order v2*) all match the folder.
- **Links page**: Step 2 key-check response spreadsheet recorded; v2 form links marked verified Aug 7. No EDIT link found anywhere reviewer-facing.
- **Session 9's four "not a defect" notes honoured**: duplicate grammar file (resolved to `OLD/`), kanji module untouched (deliberately blocked), vocabulary module unrendered (declared first draft), two restored tracker rows (history intact).
- **Known-open, not new** (already tracked, no action here): the 6 N4 stress-test words invisible to the ledger regex (today's regenerated report still shows 87 vs 93 bank words — the regex fix from Session 9's defect list hasn't landed); the Step 2/3 cloned bank (P0 row exists); Session 9 handover's "Open for you" item 1 still says "Delete n5-practice.jsx" — superseded within the same file by the resolved section (the file now sits at `OLD/n5-practice.DEFUNCT.jsx`, deletable only by hand).

*Next week can skip: kana numeric claims (unless the kana files change), revisit-map id/step verification (unless grammar-module.jsx changes), the Links page v2 form set.*
